# Password guests receive on their physical invitation
WEDDING_PASSWORD=your-password-here

# Secret for signing the unlock cookie (generate with: openssl rand -base64 32)
COOKIE_SECRET=your-cookie-secret-here
